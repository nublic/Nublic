<module>
	<!--
	    This file is automatically updated based on new dependencies by the
	    goal "vaadin:update-widgetset".
	-->
	
	<source path="mediaplayer">
		<exclude name="Clip.java" />
		<exclude name="ClipContainer.java" />
		<exclude name="Mediaplayer.java" />
		<exclude name="MediaplayerTestApplication.java" />
	</source>

	<!-- Inherit DefaultWidgetSet -->
	<inherits name="com.vaadin.terminal.gwt.DefaultWidgetSet" />	
	
	<!-- Widgetset default theme -->
	<!-- This is relative to widgetset_directory/public -->	
	<script src="jquery.min.js"></script>
	<script src="projekktor.min.js"></script>
	<stylesheet src="theme/style.css"></stylesheet>

    <!--
     Uncomment the following to compile the widgetset for one browser only.
     This can reduce the GWT compilation time significantly when debugging.
     The line should be commented out before deployment to production
     environments.
      
     Multiple browsers can be specified for GWT 2.0.3 as a comma separated
     list. The supported user agents at the moment of writing were:
     ie6,ie8,gecko,gecko1_8,safari,opera
     
     The value gecko1_8 is used for Firefox 3 and later and safari is used
     for webkit based browsers including Google Chrome.
    -->
  	<set-property name="user.agent" value="gecko1_8,safari"/>
    
</module>
