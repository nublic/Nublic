#!/usr/bin/make -f

DEB_PYTHON_SYSTEM=pysupport

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/python-distutils.mk

# Add here any variable or target overrides you need.

override_dh_install:
	dh_install
	mv debian/scamall-notification/usr/share/pyshared/scamall/notification/main_client_terminal.py debian/scamall-notification/usr/bin/scamall-notification

